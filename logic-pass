Q1/Write a method that will remove any given character from a String?
  ans: Using the replace() method

Q2/Write a program to find all prime numbers up to a given range of numbers?
  ans: 
for x in range(first,last) :                  #enter the first and last number of the range you want
  if x > 1:
    for i in range(2,x):
        if (x % i)== 0:

            break
    else:
            print(x,"is a prime number")
  else :
    print(x,'is not a prime number')
  
  
Q3/write a function that count how many the given character repeated in a given string?
    ans: 
string = 'enter any string you want'
print('enter the character that you want to count')
char=input()
print (char,'is repeated',string.count(char),'times')
